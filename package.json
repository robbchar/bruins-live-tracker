{
  "name": "bruins-live-tracker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test:web": "pnpm -C admin-web test",
    "dev:web": "pnpm -C admin-web dev",
    "watch:web": "pnpm -C admin-web test --watch",
    "test:functions": "pnpm -C functions test",
    "watch:functions": "pnpm -C functions test --watch",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
